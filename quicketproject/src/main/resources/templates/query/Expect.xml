<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.client.qna.dao.QnaDao">

	<!-- 게시판 리스트 조회 -->
	<select id="qnaList" parameterType="qna" resultType="qna">
		SELECT q_no, q_title, q_content, u_id, to_char(q_regdate, 'YYYY-MM-DD') as q_regdate
		FROM qna
		ORDER BY q_no desc
	</select>
	
	<!-- 게시물 등록 -->
	<insert id="qnaInsert" parameterType="qna">
		INSERT INTO qna(q_no, u_id, q_title, q_content)
		VALUES(#{q_no}, #{u_id}, #{q_title}, #{q_content})
	</insert>
</mapper>